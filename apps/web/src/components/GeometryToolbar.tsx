interface GeometryToolbarProps { onUndo: () => void; onRedo: () => void; onSave: () => void; onOpen: () => void; onAddPoint: () => void; onAddLine: () => void; onAddCircle: () => void; onAddArc: () => void; creationMode: "line" | "circle" | "arc" | null }

export function GeometryToolbar({ onUndo, onRedo, onSave, onOpen, onAddPoint, onAddLine, onAddCircle, onAddArc, creationMode }: GeometryToolbarProps) {
  return <div className="toolbar" aria-label="几何工具栏"><button className="primary" aria-label="添加点" onClick={onAddPoint}>＋ 添加点</button><button>选择</button><button aria-label="添加直线" aria-pressed={creationMode === "line"} data-active={creationMode === "line"} onClick={onAddLine}>直线</button><button aria-label="添加圆" aria-pressed={creationMode === "circle"} data-active={creationMode === "circle"} onClick={onAddCircle}>圆</button><button aria-label="添加圆弧" aria-pressed={creationMode === "arc"} data-active={creationMode === "arc"} onClick={onAddArc}>圆弧</button><button onClick={onUndo}>撤销</button><button onClick={onRedo}>重做</button><button onClick={onSave}>保存 .mgeo</button><button onClick={onOpen}>打开 .mgeo</button></div>
}
