interface GeometryToolbarProps { onUndo: () => void; onRedo: () => void; onSave: () => void; onOpen: () => void; onAddPoint: () => void; onAddLine: () => void; onAddSegment: () => void; onAddCircle: () => void; onAddArc: () => void; onDelete: () => void; onToggleLock: () => void; onSelectTool: () => void; creationMode: "line" | "segment" | "circle" | "arc" | null; hasSelection: boolean; allSelectedLocked: boolean }

export function GeometryToolbar({ onUndo, onRedo, onSave, onOpen, onAddPoint, onAddLine, onAddSegment, onAddCircle, onAddArc, onDelete, onToggleLock, onSelectTool, creationMode, hasSelection, allSelectedLocked }: GeometryToolbarProps) {
  return <div className="toolbar" aria-label="几何工具栏"><button className="primary" aria-label="添加点" onClick={onAddPoint}>＋ 添加点</button><button aria-label="选择工具" aria-pressed={creationMode === null} data-active={creationMode === null} onClick={onSelectTool}>选择</button><button aria-label="添加直线" aria-pressed={creationMode === "line"} data-active={creationMode === "line"} onClick={onAddLine}>直线</button><button aria-label="添加线段" aria-pressed={creationMode === "segment"} data-active={creationMode === "segment"} onClick={onAddSegment}>线段</button><button aria-label="添加圆" aria-pressed={creationMode === "circle"} data-active={creationMode === "circle"} onClick={onAddCircle}>圆</button><button aria-label="添加圆弧" aria-pressed={creationMode === "arc"} data-active={creationMode === "arc"} onClick={onAddArc}>圆弧</button><button aria-label="删除对象" disabled={!hasSelection || allSelectedLocked} onClick={onDelete}>删除</button><button aria-label={allSelectedLocked ? "解锁对象" : "锁定对象"} disabled={!hasSelection} onClick={onToggleLock}>{allSelectedLocked ? "解锁" : "锁定"}</button><button onClick={onUndo}>撤销</button><button onClick={onRedo}>重做</button><button onClick={onSave}>保存 .mgeo</button><button onClick={onOpen}>打开 .mgeo</button></div>
}
