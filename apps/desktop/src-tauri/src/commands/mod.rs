//! **IPC 命令的分组**（从 `lib.rs` 拆出，评审方案 2）。
//!
//! 这一层的每一条命令都只做三件事：从托管状态里取自己需要的那一份、调仓储 / 代理 / 凭据库、
//! 把结果或**能被前端读懂的失败**交回去。所以按"它碰的是哪一份状态"分组 —— 那正好也是审这一层时
//! 想问的问题（"谁能读到密钥"、"谁能写库"、"谁能停掉代理"）。
//!
//! ## 拆成模块**不改变**命令的可见面
//!
//! 唯一的 `generate_handler!` 仍然在 `lib.rs`，命令清单仍然**逐字列在** `tests/shell_smoke.rs` 里
//!（那份清单现在扫的是整个 `src/`，所以命令搬到哪个文件都不影响"新增命令必须在那里写下名字"这条约束）。

pub mod proxy;
pub mod secrets;
pub mod conversations;
pub mod repository;
pub mod providers;
