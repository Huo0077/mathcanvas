//! **回环代理**（Task 1.5）。
//!
//! 目前只有 `security.rs`（判据层）。如实标注**还没有起真服务器**：
//! `bind(127.0.0.1:0)` + 路由分发需要异步运行时（tokio / hyper），
//! 而本轮交付的是"**判据全部就位且被测**"。
//!
//! 这个切法的理由与 Task 1.4 同源，但在安全组件上更硬：**先写服务器再补判据**，
//! 每一条规则都会退化成"大概拦住了" —— 因为测它们要起九次服务器，
//! 于是没人会为九种拒绝理由各写一条用例。抽成纯函数之后，
//! 每一条拒绝理由都能被确定性地钉住（`tests/proxy.rs`）。

pub mod security;
