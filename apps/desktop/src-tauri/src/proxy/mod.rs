//! **回环代理**（Task 1.5）。
//!
//! 两层，各管一件事：
//! - `security.rs` —— **判据**（九种拒绝理由、SSRF 防线、重定向不传凭据、脱敏）。纯函数，16 例。
//! - `server.rs` —— **真服务器**（绑回环、路由、令牌、体积上限、取消句柄）。见那边的"如实标注"。

pub mod security;
pub mod server;
