# MathCanvas

MathCanvas 是一个面向数学与工程场景的 2D 交互绘图工作台原型。项目采用 React、TypeScript 和 Vite 构建，并将 Geometry DSL、数值几何内核、Scene Graph 与 SVG 工作台分层，便于持续扩展和多人协作。

## 当前状态

**统一 Ribbon 基线、后续 UI 优化、工程制图视觉重做与 CAD 2D 绘图交互重做均已完成**（2026-09-16）：微积分工作区标签已退役，当前新会话默认打开内部 `conics` 工作区，界面标签为「平面几何」；含微积分图元的旧 `.mgeo` 仍可打开。P0 技术验证、P1 数学内核、P2 交互、P3 函数分析、P6 立体几何、P7 工程制图与 Ribbon UI 基线均已完成；Ribbon 后续优化完成平面几何改名、A/B/C 统一点名、3D 画布点名、重命名入口前移、右侧面板精简（移除约束与智能体展示，保留文档数据）、CAD 画布随停靠面板扩大、法向量/二面角底部提示，并修复操作指引浮层遮挡底部状态栏的问题；随后完成**工程制图视觉重做**（制图台 + 图纸层次、图框与图签、视图框角刻度、显式缩放读数、单列响应式修复）与**CAD 2D 绘图交互重做**（稳定坐标窗口、橡皮筋预览、八类对象捕捉与 Tab 循环、栅格捕捉、夹点编辑、方向框选、坐标键入与动态输入、线宽与命中带修复、偏移/修剪/延伸）。CAD 工作区仍保留分层命令栏、模型/图层/图纸树、可持久化图纸视口、2D 直接绘图模式、上下文 Inspector、四视图、投影线联动、工程标注以及 SVG/DXF/PDF 矢量导出。

### 统一工作区界面

- 顶部采用约 40px 系统栏、32px 工作区标签栏和约 80px Word 风格 Ribbon；平面（显示为「平面几何」，内部 ID 为 `conics`）、立体几何与工程制图共享命令分组，避免重复的旧工具栏。多模态组只保留「文字转换」与「图片转换」两个入口，转换服务尚未接入，因此两个入口保持禁用并说明原因。
- Ribbon 支持按钮或 `Ctrl + F1` 折叠、标签临时呼出、图钉固定；右侧 Inspector 根据选择状态展示属性，并在无选择时保持精简。右侧面板不再显示几何约束和智能体（Agent）；约束数据、校验与 `.mgeo` 编码保持不变，教学测量入口位于默认可见的数据区。
- 工作区限制在 `100vh`；390px 手机视口提供对象列表和属性检查器抽屉，画布与状态栏保持在视口内。底部提示随工具和创建步骤变化。

完整进度、各项根因与验证证据见 [`docs/project-progress.md`](./docs/project-progress.md)。

## 快速开始

```bash
npm install
npm run dev
```

启动后打开 Vite 输出的本地地址即可使用工作台。Windows PowerShell 也可以使用对应的 `npm.cmd` 命令：

```powershell
npm.cmd install
npm.cmd run dev
```

在本地仓库中也可以直接执行（当前检出位于 `D:\数学画布\mathcanvas-main`）：

```powershell
Set-Location D:\数学画布\mathcanvas-main
npm.cmd run dev
```

### 浏览器验收

```bash
npm run build
npm run test:e2e
```

`test:e2e` 由 Playwright global setup **先按 `apps/web` 的 vite 配置重新构建** `build-check/mathcanvas-current`，再在同一 Node 进程中启动本地预览服务，测试结束后自动关闭——这样 e2e 验证的是当前工作区；手工验收时刷新 `127.0.0.1:4175` 上那份预览即可看到同一份产物。手动测试时，先运行 `npm run dev`，再在浏览器打开终端输出的地址。

### 文件保存与打开

- “保存 `.mgeo`”会下载当前几何文档；“打开 `.mgeo`”通过浏览器文件选择器读取本地文件。
- 工作台会在浏览器本地保存草稿，用于刷新或重新打开后的恢复。
- 当前没有服务端文件上传接口；如果需要云端上传，需要单独设计鉴权、存储和版本管理流程。

## 已实现能力

### 画布与图元

- 创建、选择、拖动、框选、Shift 多选、删除、锁定、分组和对齐。
- 支持点、直线、线段、射线、折线、圆、圆弧、抛物线、椭圆、双曲线和函数图像。
- 支持拖动图元本体及控制点；移动端点后，使用点 ID 的关联图元会自动更新。
- 画布横向填充工作区，支持撤销/重做、显隐控制和本地草稿自动保存；默认中心为 `(0, 0)`，可使用鼠标中键拖动或 `Space + 左键拖动` 平移到更远坐标，并支持滚轮以指针为中心缩放以及画布右下角的「放大 / 缩小 / 重置视图」按钮（0.05×～40×）。**网格是固定尺寸的**：一格恒为 1 个世界单位、每 10 格一条主线，缩放只改变**可见范围**（与 3D 背景坐标系同一套语义，见 `apps/web/src/sceneGrid.ts`）。

### 属性检查器

- 公共样式：颜色、填充色、线宽、透明度和线形。
- 直线：斜率、截距、方向和端点信息。
- 圆与圆弧：圆心、半径、起止角和范围。
- 抛物线、椭圆、双曲线：中心、顶点、焦点、轴向、离心率及渐近线等关键特征。
- 函数图像：公式、定义域、采样状态、值域提示和采样交点。
- 属性栏会根据当前选中的图元切换为对应的特异化字段，并保留统一的样式编辑入口。

### 函数与公式（入口在当前平面工作区）

- 工具栏「添加函数」直接创建函数图像，属性栏提供常用函数预设下拉（指数、对数、三角、双曲、高斯等），选择预设会同时套用它的课堂定义域。
- 公式框支持直接输入和光标位置插入，不依赖单独的函数选择器。
- 计算器式快捷键盘支持常见函数、常数、运算符和括号的快速插入。
- 支持嵌套与复合表达式，例如 `sin(ln(x))`、`exp(-x^2) * cos(2*x)` 和 `a*sin(b*x+c)+d`。
- 支持常用形式 `e^x`、`|x|`、`sqrt(x)`、三角函数、反三角函数、双曲函数以及带底数的 `log_b(x)`。
- 处理无定义值、无穷值和不连续区间时分段采样，避免错误连接渐近线两侧的曲线；对未被采样点正好命中的渐近线执行跳变检测，避免绘制竖直连线。
- 在公式框内按 `Backspace` 或 `Delete` 只编辑文本，不会误删函数图元。
- 选中函数后可创建导函数、切线和积分区域；**函数与它的派生分析对象作为一个对象一起删除**，因此含微积分图元的旧 `.mgeo` 里函数可以直接删掉。

### 点、交点与轨迹

- 3D 新建点自动提供 A、B、C 等课堂常用标签；2D 新建点同样使用 `A`、`B`、`C`…`Z`（`Z` 之后回退为 `P27`、`P28`…）。选中任一图元都可以在属性栏默认展开的「几何参数」区直接改名（原先需要先展开「外观样式」）。
- 鼠标悬停图元时显示当前接触点的画布坐标。
- 图元相交时显示交点坐标；交点默认是派生结果，不会自动创建为独立图元。
- 采样曲线会显示**全部**交点（上限 64 个），保存其中一个只隐藏那一个，同一对图元的其他交点继续可见可点。
- 用户可以将交点保存为持久化点，并继续用于标注、连线或其他几何构造。
- **动点（平面几何，2026-09-17 重做）**：任何点都可以绑定到一条曲线（直线 / 线段 / 射线 / 折线 / 圆 / 圆弧 / 函数图像 / 椭圆 / 抛物线 / 双曲线）成为动点，绑定参数是该曲线的**自然参数**（直线是仿射比例、圆与椭圆是角度、函数图像是 x 本身、抛物线与双曲线是轴向参数），所以「能滑多远」由曲线自己决定：直线无限、射线只向前、线段与折线有界、圆与椭圆整周。**两条驱动通路写同一个参数、完全等价**：画布上直接**拖动**、属性栏**「路径参数」输入框**（原「动效演示」栏的滑块与播放已按用户要求删除）；抛物线与双曲线另有可编辑的「参数域」，双曲线可选择「分支」。
- **参数是唯一真值、坐标是派生缓存**，因此动点永远精确落在约束上，不会在连续拖动后漂离曲线；几何对象构成有向无环图，重算按**拓扑序**一趟完成，测量只在其来源真的变化时重算。**点永远赢过派生曲线**：连线的可见线会穿过它两端的点、轨迹必然穿过动点自己，所以点的命中区画在所有派生曲线之上（连线的命中带还从两端各缩进 16px）——否则动点及其相连的定点正中心那一下会落到连线上，点就"抓不住"了。
- **轨迹（locus）与平面测量**：对选中的动点「记录轨迹」，采样按**曲率自适应**细分，并在渐近线、间断点与约束域边界处**切分为多条分支**（跨越渐近线那条本不该存在的竖线不会被画出来）。选 2 个点量长度，选 3 个点量角度（第二点为顶点）/ 面积 / 垂距，读数随动点移动实时更新，退化几何标记为 `degenerate` 而不是给出误导性的 0。
- 选中两个已标记点后，可以创建保持点引用的线段、直线、射线或折线；抛物线连接需要额外提供顶点、轴向、焦参数或第三点。

### 操作指引

- 点击功能键（添加图元、空间构造、测量、约束、函数分析等）后，画布左下角出现一条**小尺寸**指引，说明这一步该点什么、要按哪个键（`Esc` / `Shift` / `Alt`）或刚创建了什么。
- 指引只在点击功能键时出现：切换工作区、按 `Esc`、点右侧 `×`，或一步创建动作完成时自动消失；点另一个功能键会被新指引替换，不会变成常驻说明面板。
- 预置条件不足时给出可执行的做法，而不是只说「选择不对」的报错——例如二面角会提示「按住 Alt 点实体表面单独选面，Shift 加选第二个面」。
- 选中立方体、棱锥、圆柱、圆锥会提示 `Alt + 点击` 可单独选中棱或面（二面角与剖切都依赖这个入口）。
- 3D 画布的「法向量」与「测量二面角」开关会把状态写给底部状态栏：法向量说明它显示的是各可见面的外法向量；「测量二面角」明确说明画的是**坐标轴夹角的示例值**，并提示真实测量步骤（`Alt` 单选两个面，再点属性区的「二面角内角 / 二面角外角」）。

### 三维几何工作区

- 切换到“立体几何”工作区后，可创建空间点；新点自动使用 A、B、C 等课堂标签，并把点名直接标注在 3D 画布的点位旁（标注层不拦截点击，随相机旋转、缩放与窗口尺寸更新）。
- 空间点支持 `x/y/z` 坐标编辑，点位置和稳定对象 ID 是三维构造的真源。
- 选择空间点后可创建由点 ID 驱动的空间直线、空间平面和空间面；来源点变化时关联图元自动重算。空间点还可以绑定到**宿主**（空间直线 / 线段 / 射线 / 棱 / 面 / 圆柱与圆锥侧面）并严格沿宿主滑动，或绑定到**实体的内部**（可在立方体内自由移动、越界会被夹回表面）；两种绑定在实体移动时都会让点跟着重算（2026-09-17 修：模板物化出来的子对象此前不声明依赖所属实体，导致"实体动了、绑在它面上的点还留在原地"）。
- 立方体、棱锥、圆柱和圆锥仍作为参数化快捷模板保留；**圆柱与圆锥默认 48 段**（多边形近似，24 段时轮廓看得出棱），分段数可在属性栏改（3–256）。四类模板都支持**三轴朝向**（属性面板按度输入，可精确填 45°/90°，并有「归零」与各轴「+90°」快捷按钮），旋转按 X → Y → Z 依次绕图形自身中心生效，朝向属于文档状态并随 `.mgeo` 保存。点、棱、面和多面体支持点驱动拓扑编辑，模板生成的拓扑也可继续进入课堂构造流程。
- Three.js 负责 3D 场景渲染，支持点、线、面选择元数据、网格、坐标轴、透明面、隐藏边、法向量、视角控制和重置。世界坐标采用 **Z 轴朝上**（网格为 XY 地面，蓝色 Z 轴竖直向上）。
- 空间拾取按“点 > 棱 > 面 > 线 > 实体”优先级返回稳定 ID、子部件 ID（展开面）、深度和世界坐标；约束覆盖点在线/点在面/共线/共面/平行/垂直/固定距离并显示残差与冲突解释（当前为诊断，不做投影求解）。
- 教学测量覆盖长度、距离、角度、面积、体积和二面角，统一标注来源、精度与状态，来源点移动后自动重算；二面角报告面内内角并可切换外角，画布显示实际数值标签、公共棱、角弧和法向量，代数区提供可展开的顶点/棱/面子树和教学测量分组。
- 通用剖切支持任意点驱动多面体与四类模板：平面按面边界求交、去重并按面邻接串联边界，分类为无交/点交/线交/多边形交，多边形截面带半透明填充和有序边界线，退化截面绘制线段；默认剖切平面穿过来源包围盒中心，来源实体受删除保护。
- **交点 / 交线 / 交面是三个各自独立的图元**（2026-09-17）：画布自动枚举场景里**所有两两相交的顶层实体**，每一对给出三类可独立点击的预览——虚线**交线**（两个表面的公共边界）、**交集的每一个面各一份**半透明**交面**片、**交线的每个拐点各一份**圆点**交点**。点虚线创建 `intersectionLine`（段数 / 总长度），点哪块面片就把**那一面**建成 `intersectionFace`（一个表面：面积与法向读数，**填充色可改**），点圆点建成跟随两个来源重算的 `intersectionPoint3`。三者都进对象列表、随来源重算、删来源时级联注销。完全包含时两个表面不相交，因此只有交面、没有交线与交点。性能上按来源几何签名缓存每一对的结果（只动一个实体时其余对直接沿用），包围盒先筛掉不相交的对，单次扫描最多算 12 对来源的布尔交集、每对最多 64 个面 / 32 个交点（状态栏会说明没画全的部分）。布尔交集只对凸实体成立，非凸输入给诊断而不是近似。
- **截面 / 交线也可以"点"出来**：选中含面环的对象时画布立即出现**虚线预览**——只选一个实体时预览默认剖切平面的截面。悬停高亮并提示「点击创建为图元」，**点击即创建**持久化的截面 / 交线图元（进文档、可撤销、随来源自动重算）；顶点手柄默认优先于预览，两个例外是截面预览与**交点标记**（交线的拐点常常就是来源的顶点，两者共心）。求交在内核 `intersections3d.ts`（两个面环各自被对方平面裁剪成区间后求区间交，共面直接短路，重复线段去重），布尔交集在内核 `boolean3d.ts`（凸实体半空间依次裁剪，切口面按平面内角度排序，并给出每个面自己的法向与面积）。
- 展开/折叠已泛化到任意拓扑：按共享棱邻接从根面递归展平，支持连续折叠进度、展开方向与面朝向无关、重叠诊断，展开图每个面可单独拾取；折叠姿态是临时状态，reduced motion 下直接切换。
- 二面角按公共棱报告面内内角，属性栏可选内角/外角，测量说明给出两个角度与公共棱；选中来源面时画布绘制公共棱、角弧和朝外法向量标记。
- 3D 文档继续使用 `schemaVersion: "0.1"`，旧 2D `.mgeo` 文件保持可读。

### 工程制图工作台

- 经典 CAD 层次：顶部任务类别命令栏（选择、创建、修改、标注、检查、导出）+ 二级命令面板与「返回」，文件、撤销、重做和保存在全局头部。
- 左侧停靠面板提供模型树 / 图层树 / 图纸树三个标签页，共享名称过滤；图层树支持层级、当前层、显隐、锁定、新建子图层和删除（至少保留一个几何图层）；图纸树按图纸展开视图，显示视图类型、比例和来源对象。
- 中心区域有两种模式：`3D 投影` 展示主视图、俯视图、左视图和轴测图四张图纸视口（投影线开关是临时状态），`2D 绘图` 提供可直接点击绘制的绘图视口。
- 视图标题压在图内左上角（`工程视图` + 视图名），空视图只在标题右侧标注「空视图」，不再绘制坐标轴占位板，也不会挡住第一笔点击。
- 2D 绘图新建的点、直线、线段、射线、折线、圆和圆弧写入当前图层；当前图层被隐藏或锁定时拒绝创建，并在底部状态栏说明原因。
- **投影来源可切换**：图纸默认投影自己的 CAD 文档，也可以切换为「立体几何」工作区文档（两者是相互独立的文档，这也是此前「一直显示暂无可投影的空间对象」的原因）。按钮显示当前来源；空状态会分别说明「本图纸没有可投影对象；立体几何里已有模型」或「立体几何工作区还没有可投影的对象」，并给出「去立体几何」的动作。图纸布局（纸张、视图、比例）始终属于 CAD 文档，切来源不会改版式。

### CAD 2D 绘图交互

- **命令条在图纸之外**：坐标输入、长度/角度、角度约束与栅格捕捉开关、偏移/修剪/延伸都在图纸上方那一行工具条里，图纸内不再堆第二行按钮；读数文字按视图 `viewBox` 跨度换算字号，缩放图纸时不会被放大成巨大文字。工具条必须保持单行——换行会占掉纸张高度（实测把纸张从 128% 压到 64%）。
- **稳定的绘图坐标系**：2D 绘图用固定坐标窗口（以原点为中心、只随视图比例缩放），不再按内容自适应——画下第一个点坐标系不会跳，标题栏的 −/＋ 真正生效；带主/次栅格（缩小时自动加粗）与坐标轴。
- **对象捕捉**：端点 / 交点 / 中点 / 圆心 / 象限点 / 垂足 / 切点 / 最近点，12px 捕捉半径，带捕捉标记与类型标签；优先级即此顺序，`最近点` 垫底（否则它在数学上永远最近会吃掉其他候选）；命中多个候选时按 **Tab** 循环切换。捕捉始终优先于栅格与角度约束。
- **实时反馈**：创建过程中显示橡皮筋预览（与最终形状同构）与「长度 · 角度」读数；`自由 → 正交 → 45° 极轴` 可切换，正交始终压到轴上，**极轴仅在指针偏离射线 4° 以内才吸附**，按住 `Shift` 可临时正交；**栅格捕捉**是独立开关（取最细可见网格，放大到 1mm 次网格时精度随之提高）。
- **坐标精确化**：视口工具栏可直接键入落点——绝对 `12,34`、相对 `@10,5`、极坐标 `@20<45`（接受空格与全角逗号）；创建进行中另有「长度 / 角度」字段，只改一个维度、另一个沿用当前指针。输入非法只说明，不落点。
- **夹点编辑**：选中图元显示控制点（端点 / 折线顶点 / 圆心半径 / 圆弧起止角），拖控制点改几何、拖本体整体平移；拖动期间用临时文档预览，指针抬起才提交成一次可撤销改动。夹点几何与数学画布共用 `interaction.ts`。
- **方向框选**：左→右为窗口选择（只选完全包含的对象，实线框），右→左为相交选择（绿色虚线框）；判定在内核 `selection.ts`。`Esc` 分级：先取消创建/命令，再关指引，最后才清空选择。
- **偏移 / 修剪 / 延伸**：偏移按给定距离**新建**平行对象（折线按相邻偏移线求斜接，圆/圆弧改半径）；修剪保留目标 a 端所在的一半，延伸把目标 b 端拉到边界，两者都要求选中 2 个图元且顺序为「先边界、后目标」。几何在 `packages/geometry-kernel/src/editing.ts`，前置条件与补丁组装在 `apps/web/src/draftEditing.ts`；圆/圆弧的修剪仍不在范围内，遇到会明确报错。
- **可见性与命中**：工程图线宽是像素量级（此前 `non-scaling-stroke` 下写成 `0.022` 实测渲染成 `0.04px` 的隐形线），并为绘图与投影图元各加一条 **14px 透明命中带**；创建进行中点在图元上是**落点**而不是选中。

- 图纸布局（纸张、标题栏、视口位置、尺寸、比例、显隐）属于文档状态，保存进 `.mgeo` 并参与撤销/重做；当前树标签页与展开节点属于本地工作台偏好，不写入文档。
- 右侧上下文 Inspector 分为数据、外观、工程标注三个页签（约束与智能体展示已移除，约束数据仍保留在文档里）；无选择时显示当前图纸、视图、图层、单位和命令提示，并列出工程标注/图纸视图引用到的来源对象，已删除的来源标记为「来源已删除」。教学测量入口位于数据页签，保留真实测量能力。
- 左侧或右侧停靠面板收起后，中间画布会吃掉空出的列宽，不再留下空白列。
- 图纸按制图台可用区域等比缩放并居中，工具栏提供「适应窗口 / 缩小 / 放大」与比例读数：自动适应是默认，可手动覆盖，放大超过一屏后整张图纸可滚动查看而不是被裁掉（缩放用 CSS `zoom`，滚动范围与点击命中始终一致）。
- 工程制图是「制图台 + 图纸」的层次：画布是带淡格线的冷灰台面，图纸是白纸，图框内缩带分区刻度，右下角是图签（工程图纸 / 图幅 / 比例 / 视图数 / 显示比例）；四个视图框用细线与双角刻度表达"视图"，视图比例与显隐按钮悬停时才出现，触摸设备常驻。
- 投影几何继续复用 P7 的 renderer-neutral 描述；隐藏的视图不参与导出，也不会生成伪造几何。
- 旧 `.mgeo` 文件在解码时迁移为默认几何/尺寸/辅助线/注释图层、默认 A4 横向图纸和四个 P7 视图。

### 导出与约束诊断

- 参数变化、拖动与依赖重算都由依赖图的**拓扑增量重算**驱动；动点由画布拖动与属性栏「路径参数」输入框两条**等价**通路驱动——原「动效演示」栏的滑块与播放按钮已按用户要求删除，逐帧播放的界面入口不再存在（内核里的动画计算模块保留并有单测）。
- 支持 `.mgeo`、SVG、CSV 和 PNG 导出，并提供导出失败反馈；CSV 以 UTF-8 BOM 开头（Excel 打开中文标签不再乱码）；CAD 工程制图工作区额外支持 SVG、DXF、PDF 矢量导出（**导出全部视图**，PDF 对 WinAnsi 之外的字符替换成 `?` 而不是整体失败），立体几何工作区仍禁用 SVG/PNG 投影导出并说明原因。
- CAD 工程制图提供主视图、俯视图、左视图和轴测图，四视图共享稳定源 ID 与选择状态；工程标注从点 / 棱来源计算线性尺寸、角度和公差，并在来源变化后自动重算。
- 约束的残差、冲突与退化诊断在**内核**里（`constraints3d` / `solveLineConstraints`，含 2026-09-17 新增的"退化目标列入 `unsatisfiable`"）并有测试覆盖；右侧的约束列表面板已按用户要求移除，界面上不再有约束列表入口。

## 包结构

- `packages/dsl`：版本化 Geometry Document、图元类型、校验和 `.mgeo` 编解码。
- `packages/geometry-kernel`：数值策略、约束、交点、射线/折线、圆锥曲线和微积分计算。
- `packages/scene-graph`：Domain Operation、Patch 校验、依赖传播和事务回滚。
- `apps/web`：工作台 UI、SVG/Three.js 画布、交互状态、属性检查器和文件导入导出。

## 验证命令

```bash
npm test
npm run typecheck
npm run build
npm run test:e2e
```

当前验证基线（2026-09-17，全身大体检十批、e2e 构建修复、平面网格固定与"圆上四个点"之后实测）：`npm.cmd test` 为 107 个测试文件、1202 个用例通过；4 个 workspace 类型检查通过；ESLint 0 个 error、14 条 warning；Web 生产构建通过（Vite 因此仍提示主 bundle 超过 500 KB）；Playwright Chromium 85/85 通过（global setup 现在**按当前工作区重新构建** `build-check/mathcanvas-current` 再预览，因此这份结果对应的是工作区源码，而不是该目录里上一次构建的产物），覆盖 Ribbon、跨工作区操作、CAD 图纸填充与显式缩放、CAD 2D 绘图交互（命令条在图纸之外 / 固定坐标窗口 / 橡皮筋预览 / 端点与切点捕捉 / 栅格捕捉 / 夹点编辑 / 方向框选 / 坐标键入 / 线宽与命中带 / 偏移与修剪延伸）、工程制图的投影来源切换与投影、立体几何的自动交点 / 交线 / 交面预览与点击创建对应图元（`e2e/three-intersection-previews.spec.ts`）、截面虚线预览（只画交线与交点，不铺大剖切面）、**3D 视口的渲染管道不重建（编辑 / 选择 / 展开 / 拖动期间 renderer 只建一次）、场景内容按签名增量同步（展开与选中不再整场重建）、相机跨工作区保留、绑定点沿宿主拖动、自动取景（含"编辑时不抢视角"）、背景与平面网格严格 1 格 = 1 世界单位（缩放只改变覆盖范围；平面侧由 `e2e/planar-grid-cell.spec.ts` 在浏览器里量相邻网格线的世界间距恒为 1）、**圆柱 / 圆锥的圆上只保留 4 个象限点（48 段近似不再冒出 96 个点标记，`e2e/three-round-solid-points.spec.ts` 断言点标签恰好 8 个、对象列表也是 8 行）、交点标记只标有意义的角点且光滑交线补齐到 4 个**、圆柱与圆锥默认 48 段、动点绑定宿主与「实体内」约束（越界夹回表面）、实体移动时绑定点跟随、四个模板的默认截面与 45° 倾斜、画布尺寸在文案变化与窄屏下都不被压缩、平面画布拖动动点时交点预览走增量（拖动不再卡顿）**，以及 1440px/768px/390px 视口。平面几何动点系统（曲线约束、依赖图拓扑重算、平面动态测量、轨迹分支切分、删除级联）当前由单测与 App 用例覆盖，另有 `e2e/planar-drag-performance.spec.ts` 守住拖动增量、`e2e/planar-connected-point-drag.spec.ts` 守住"动点与连线相连时点仍然抓得住、拖轨道只带动动点"。Vitest 的 jsdom 3D 测试仍会输出 WebGL context 未实现提示。首次运行需先执行 `npx playwright install chromium`，否则会报缺少浏览器可执行文件。

工程工作台 Task 1-7 的聚焦验证：`LayerTree`/`DrawingTree`/`CommandBar`/`EngineeringWorkbench`/`DrawingViewport`/`DrawingSheetView`/`EngineeringInspector` 等新增测试文件 7 个；DSL 与 Scene Graph 图层/图纸操作 3 个测试文件、41 个用例；`e2e/engineering-workbench.spec.ts` 覆盖旧文档迁移、2D 绘图写入活动图层、图层隐藏、刷新后布局保持、隐藏视图不导出、键盘操作、图纸填充与显式缩放（Task 14）。

## 项目文档

- [项目进度](docs/project-progress.md)：**单一进度记录**——各阶段完成项、每轮 RED→GREEN 证据、验证数字、误报清单与下一步。
- [功能目录](docs/feature-catalog.md)：记录当前可用能力、规划能力和明确限制。
- [主实施计划](docs/multimodal-math-engine-implementation-plan.md)：P0–P8 的阶段划分与**逐阶段落地状态**（P4 / P5 已排除）。
- [GitHub 调研](docs/research/graphing-tools.md)：记录对 GeoGebra、JSXGraph、function-plot 等同类项目的功能与架构调研。
- [制图交互调研](docs/research/drafting-interaction-patterns.md)：记录 AutoCAD / 制图类工具的捕捉、夹点、命令流程等交互模式。
- [第一周验收记录](docs/acceptance/2026-09-12-week-one.md)：2026-09-12 当周的历史验收快照（数字为当时值）。
- **实施计划**（`docs/superpowers/plans/`，共 14 份，全部带 2026-09-17 复核状态）：MVP 基础、动态数学平台、公式画布、下一步数学平台、数值鲁棒性、P3+P6、点驱动 3D、3D 测量质量、工程工作台层次化、P7 工程制图、Ribbon UI、3D Auto-Fit、3D 动点宿主、3D 渲染管道去重建化。
- **设计规格**（`docs/superpowers/specs/`，共 10 份，全部带 2026-09-17 复核状态）：动态绘图平台、数值鲁棒性、点驱动 3D、3D 测量质量、工程工作台、P7 工程制图、Ribbon UI、截面 / 截线图元、3D 视口与内核重构、平面动点系统。
- 重点可以按需翻阅：[点驱动 3D 实施计划](docs/superpowers/plans/2026-09-14-point-driven-3d-geometry.md)、[工程工作台层次化实施计划](docs/superpowers/plans/2026-09-15-engineering-workbench-hierarchy.md)、[截面 / 截线图元交互设计](docs/superpowers/specs/2026-09-16-section-intersection-primitives-design.md)、[3D 视口与内核重构设计](docs/superpowers/specs/2026-09-17-3d-viewport-kernel-refactor-design.md)、[平面动点系统设计](docs/superpowers/specs/2026-09-17-dynamic-point-engine-design.md)。

## 协作约定

- 每完成一个可验证切片，同步更新 `docs/project-progress.md` 和相关功能文档。
- 提交前运行测试、类型检查、构建和相关 E2E 验证。
- 数学内核、Scene Graph 和 UI 变更保持边界清晰，避免在组件内重复实现几何算法。
- 推荐通过功能分支和 Pull Request 协作；公开仓库为 [Huo0077/mathcanvas](https://github.com/Huo0077/mathcanvas)。

## 下一步

2026-09-17 的**全身大体检**（4 路只读审计 + 性质测试 + 十批修复 + 平面网格固定）已完成并通过当前自动化验证：确认的缺陷（含 5 处阻塞级）全部修复，误报逐条记录，详见 [`docs/project-progress.md`](docs/project-progress.md) 的「全身大体检」与「体检收口」两节。

下一批候选方向：
1. 剖切平面可调（法向量与偏移量）、等长 / 等角约束（需要先定角度表示）、圆与圆弧的修剪（拆成多段圆弧）、B-rep / DWG 导入与自动尺寸布局——这些尚未编码。
2. 体检中"只记录未改"的三项：`pointercancel` 目前提交而非中止拖动（需先确认触摸平台的手势语义）；依赖图对 3/4 目标约束（共线 / 共面）不建边（属语义变更，会连带收紧"对齐锁定对象"的拒绝条件）；`boolean3d.compact` 每面一次 `sort + join` 的签名拼串（微小开销）。
3. P4 Agent 与 P5 题图解析仍在排除范围内。
