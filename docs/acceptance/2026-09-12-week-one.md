# 第一周验收记录

## 范围

对应实施计划 P0 与第一周任务清单，验证从文档到画布的最小闭环。

## 已验证

- [x] monorepo 与 `apps/web`、`packages/dsl`、`packages/scene-graph`、`packages/geometry-kernel` 边界
- [x] React + TypeScript + Vite 初始化
- [x] Geometry DSL v0.1，包含 `schemaVersion`、revision 与稳定对象 ID
- [x] 两条直线和交点的确定性计算
- [x] 参数滑块通过领域操作更新派生交点
- [x] Algebra View 显示/隐藏
- [x] 撤销/重做状态栈
- [x] `.mgeo` JSON 保存/恢复 codec
- [x] UI smoke test 覆盖默认交点和滑块更新
- [x] Domain Patch 校验：重复 ID、缺失引用、非法参数均被拦截
- [x] 工具栏添加点通过 Domain Patch 提交并同步到 Algebra View 与 SVG 画布
- [x] 可见的 `.mgeo` 打开入口接入文件选择器
- [x] Playwright Chromium smoke test：工作台加载、滑块更新、添加点
- [x] P1 表达式 AST 最小能力：数字、变量、括号和四则运算
- [x] 表达式参数链式重算，以及循环/非法表达式回滚
- [x] 圆和圆弧图元的 SVG 渲染
- [x] 直线-圆、圆-圆交点的确定性计算
- [x] 依赖 DAG 局部传播及约束 Patch 校验
- [x] 平行、垂直、重合约束的确定性几何投影
- [x] 圆、圆弧的画布多点击创建和属性编辑
- [x] 直线的画布双点击创建和端点属性编辑
- [x] `.mgeo` 文件选择与浏览器恢复流程

## 验证命令

| 命令 | 结果 |
|---|---|
| `npm test` | 8 个测试文件、27 个测试通过 |
| `npm run typecheck` | 4 个 workspace 均通过 |
| `npm run build` | Web bundle 与 3 个 package 均构建通过 |
| `npm run test:e2e` | 2 个 Chromium 浏览器用例通过 |

本轮使用项目内 Playwright Chromium 完成浏览器级验证；浏览器插件连接不影响验收结果。

## 当前边界

- 保存按钮已生成 `.mgeo` 下载，打开按钮已接入文件选择器。
- 目前“添加点”“添加圆”“添加圆弧”已完成真实操作链；直线仍是下一步的交互式图元工具。
- 圆和圆弧支持选中后编辑圆心、半径及圆弧起止角。
- 直线支持选中后编辑两个端点。
- 约束投影已支持平行、垂直和重合；多约束冲突检测与求解失败回滚仍在后续阶段。
