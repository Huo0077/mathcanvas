# MathCanvas 功能目录

这份目录记录当前可用能力、已确认的下一阶段能力以及明确的限制。它是产品说明，不替代 DSL 类型和测试。

## 当前已实现

- React/Vite 工作台、SVG 画布、Algebra View、选择、删除、锁定、分组、对齐和可见性控制。
- **截面的几何与「转为图元」**：截面是"平面与实体的真实截交**闭合多边形**"——内核按面边界求交后沿相邻面连成环，**带孔或分成多块的截面会保留全部环**（外环带半透明填充、内环只画轮廓，不会把孔洞盖住）。默认剖切面是过实体中心的**水平面**（世界 Z 轴朝上），棱锥 / 圆柱 / 圆锥的默认刀口也落在实体内部（此前它们的回退几何还是 Y 朝上的旧坐标系，刀口会切在边界上甚至切空）。选中截面后属性栏有「**转为图元**」：把每一环物化成独立的点 / 棱 / 面图元，**与来源解耦**——之后删掉截面或宿主实体都不影响它们，它们也能被移动、求交与测量。
- **3D 背景坐标系自适应，且网格严格 1:1**：栅格与坐标轴按当前相机与内容自动铺满可见范围，**格边长恒为 1 个世界单位**（1 格 = 1 单位，不随缩放变成 2/5/20 这类"好看"的值），每 10 格一条主线；缩放改变的是**覆盖范围**（按 2 的幂长大，同档内栅格连位置都不动），缩得很远时细线按"一格占多少像素"淡出、主线仍在。所以点的坐标写到 20 甚至 200，坐标系照样铺到那里，也不会出现"缩放时网格自己变大变小"、拿网格当尺子却量不准的情况；3D 画布本身也填满它所在的网格行（不再用 `calc(100vh - 420px)` 这种与真实工具栏高度无关的猜测值）。
- **3D 视口自动取景（Auto-Fit）**：相机按可见图元的世界坐标 AABB 自动构图——逐角点求出"让八个角都落进视锥"的最小距离，再留 **30% 安全边距**；近远平面随距离缩放，所以 0.01 单位的小模型和超大模型都能框得住（旧实现用包围球 × 1.25，且距离被夹在 3～60，小模型永远凑不近、大模型永远框不全）。触发时机：打开文件 / 切换工作区 / 恢复草稿，以及**内容跑出视锥**时；拟合是约 250ms 的缓出插值（系统开启 reduced motion 时直接跳变）。画布上有「自动取景」开关（默认开，偏好跨刷新保留），并且**编辑过程中永不抢视角**——只有内容越界才会重构图。
- **画布尺寸在交互中途不会变**：状态栏横跨工作台整宽，并且它所在那一行是**固定高度**（宽屏 72px，窄屏按提示行数加高）。此前状态栏被挤在左面板那一列（240px 宽、116px 高），指针一悬停到剖切面预览、提示变长，页脚就涨到 150px，而 3D 画布填满上面那一行——画布被压矮 34px 后，同一个屏幕坐标不再对应同一个世界点，于是"悬停时明明高亮了预览，点下去却没反应"。现在文案再长也不动画布（状态栏自己滚动）。窄屏（≤960px）下右侧检查器整宽另起一行但那行有 45% 的上界：属性再多也只让检查器自己滚动，3D 画布不会像以前那样被挤成 0 高度。
- **3D 动点可以绑定到宿主，并严格沿宿主滑动**：选中空间点后可把它绑定到空间直线 / 线段 / 射线 / 棱 / 面 / 圆柱与圆锥侧面（面与曲面用 u、v 两个参数）。绑定参数是**唯一真值**、坐标由参数派生，所以点永远不会漂离宿主：在画布上拖动时指针落点先投影回宿主参数、再由参数算出坐标（拖动期间可读 `data-host-residual`，实测残差 ≈ 0），下游由点驱动的线 / 棱 / 面在拖动过程中就跟着重画；一次拖动只提交一个参数，因此**一次 Ctrl+Z 精确复位**。解绑即恢复自由点，坐标字段重新可编辑；宿主参数也能直接在属性栏里键入。
- **动点还可以约束在实体内部**（2026-09-17，用户要求"动点的约束应该可以在立方体内"）：下拉里每个实体多一条「**实体内**」（`inSolid`）——点可以在体内自由移动、**出不去**：拖到外面会被沿实体表面夹回去（凸实体逐面投影，非凸输入不会给出体外的点）。参数是实体**包围盒内的三个比例** `uvw ∈ [0,1]³`（属性栏给「体内参数 u / v / w」），所以实体平移或缩放时参数不变、坐标跟着走；实体被删除时该点降级为自由点并保留位置。
- **宿主移动时绑定点会跟着走**（2026-09-17 修的缺陷）：模板物化出来的点 / 棱 / 面现在都声明**依赖所属实体**（此前只有反方向："多面体依赖它们"），所以"移动立方体、绑在它面上的点却留在原地"不再发生——从实体出发的依赖闭包会经过它的所有子对象，再到绑在子对象上的点。
- **圆柱与圆锥的默认分段数是 48**（2026-09-17，用户反馈"圆柱不圆"）：它们是多边形近似，24 段时屏幕轮廓能看出明显的棱；48 段把弦高误差降到约 0.7 像素。预览的配额（单次 12 对来源、每对最多 96 个面 / 64 个交点）与它配套，所以 48 段的圆柱参与相交时不会被"面数超限"截断。分段数仍可在属性栏里改（3–256）。
- **3D 渲染管道不再重建**：进入立体几何后 `WebGLRenderer` 与 canvas **只创建一次**，编辑、选择、展开、拖动都走命令式内容同步（`data-scene-builds` 恒为 1，`data-scene-syncs` 只在内容真的变化时增长）。此前任何一次 App 重渲染都会 `dispose()` 旧渲染器再 `new WebGLRenderer`，画面闪、开销大。
- **3D 的删除不会卡住，也不会留下悬空引用**：删除宿主实体时，被删除对象一次算清——测量 / 2D 标注 / 工程标注 / 约束随宿主注销，截面与截线等纯派生对象一并删除，分组只摘掉被删成员；**绑定在宿主上的空间点会降级成自由点并保留当前位置**，所以文档仍然合法、用户内容也不会被静默丢掉。批量选择（例如"点 + 依赖它的线"）按**并集**一起校验，不再出现"两个都删不掉"的情况；只有**锁定**仍然拒绝删除。
- **多解交点各自独立，并按屏幕距离就近吸附**：直线与曲线、圆与圆等有多个实根时，每个解是**独立实体**（解索引是任意非负整数，不再被夹成 0/1），并且记录"用户点的是哪个解"的 `hint`——形状变化导致解数组顺序翻转时，重算会取离 hint 最近的解，所以选中的那个解始终跟着走；画布上点击时按**屏幕像素距离**在命中半径内取最近的那个标记，不再由绘制顺序决定。
- **切走再切回立体几何，视角保持不变**：3D 场景组件是跟着工作区挂载/卸载的，相机现在会按"某个文档配某个视角"记在会话里——转动、缩放、平移之后切到平面几何再切回来，视角与离开时完全一致（此前会回到默认视角并重新取景）。刷新页面仍是默认视角。
- **编辑与展开不再整场重建 3D 场景内容**：每个场景对象按"内容签名"（自己的数据 + 它依赖的图元 + 影响外观的视图状态）判断要不要重建，签名没变就沿用原对象。实测展开动画的每一帧只重建那张展开网（其余 11 个对象沿用），切换选中只重建该重建的那一两个对象（此前是整场 29 个全部释放重建）。直接可见的差别是展开与选中时不再有闪动，拖动与缩放也更省。
- **3D 标注就地更新**：测量标注与空间点标注不再每次重画都整体重建 DOM，而是按对象复用同一个元素、只更新文字与位置。除了拖动更省，更重要的是读屏软件不会把同一条 `role="status"` 标注当成新消息反复播报，标注的入场动画也不会在拖动时反复重播（表现为闪烁）。
- **手工调整过的模板顶点不会被无关改动覆盖**：模板实体（立方体 / 棱锥 / 圆柱 / 圆锥）只在**它自己的参数**变化时才按参数重同步顶点；改别的对象、或手工编辑文档，都不会把已经手工调过的顶点拽回去（"全量重算"仍会按参数修复它）。
- 点、线、线段、射线、折线、圆、圆弧、抛物线、椭圆、双曲线和函数图像的基础创建或属性编辑。
- **拖动动点很跟手**：拖动时**只重算与拖动对象相关的图元对**的交点，其余交点整段沿用上一次的结果（`data-preview-pairs` 读数可以直接看到"这次重算了几对"）。旧实现每移动一次都要把全文档两两求交一遍——实测 52 个图元 / 6 条采样曲线时是 **76.5ms/次**，拖动只有约 13fps；现在拖一个点是 **0.12ms/次**，真实浏览器里帧间隔中位数 16.7ms（60fps）。被拖动对象**自己**涉及的交点仍然实时重算，不会停在原地。
- **平面几何动点**：任何点都可以绑定到一条曲线（直线 / 线段 / 射线 / 折线 / 圆 / 圆弧 / 函数图像 / 椭圆 / 抛物线 / 双曲线）成为动点。绑定参数是该曲线的**自然参数**——直线是仿射比例、圆与椭圆是角度、函数图像是 x 本身、抛物线与双曲线是轴向参数——所以"能滑多远"由曲线自己决定：直线无限、射线只向前、线段与折线有界、圆与椭圆整周。**两条驱动通路写同一个参数、完全等价**：画布上直接**拖动**（指针位置投影回曲线，点永远精确落在曲线上）、属性栏**「路径参数」输入框**。（原「动效演示」栏的滑块与播放已按用户要求**删除**：播放时只看得到起始与结束两帧，与其修不如去掉。）抛物线与双曲线额外提供可编辑的「参数域」；双曲线可选择「分支」，把点往对面拖也不会跳支。**绑定入口本身也有提示**（2026-09-17 按用户反馈补）：选中一个自由点时状态栏直接说"在右侧「路径绑定」里选一条曲线或直线，它就成为动点，之后可以直接拖它"，文档里还没有曲线时改成"先画一条路径"，已经绑定时说明拖动 / 路径参数 / 记录轨迹三种用法；属性栏下拉下面也写了同样一句。
- **轨迹（locus）**：对选中的动点「记录轨迹」，在其参数域内采样从动点轨迹。采样按**曲率自适应细分**，并在渐近线、间断点与约束域边界处**切分为多条分支**——跨越渐近线那条本不该存在的竖线不会被画出来。
- **点永远赢过派生曲线**（2026-09-17 修）：连线与轨迹都画在点之后，而连线的可见线正好穿过两端点、轨迹必然穿过动点自己——所以**点的命中区画在所有派生曲线之上**，连线的命中带还从两端各缩进 16px。没有这条规则时，"点正中心"的那一下指针按下会落到连线上；连线是派生对象、拖不动，拖动还会退化成框选，于是动点（以及和它相连的定点）看起来"抓不住"。派生曲线中段照旧可选可点。
- **依赖图与增量重算**：几何对象构成有向无环图，重算按**拓扑序**一趟完成（不再按声明顺序），且每算完一个对象就更新查找表，下游读到的是刚算出的上游值。测量只在其来源真的变化时重算。
- **平面测量**：选 2 个点量**长度**；选 3 个点量**角度**（第二个点为顶点）/ **面积** / **点到前两点连线的垂距**。读数随动点移动实时更新；退化几何（两点重合、三点共线）明确标记为 `degenerate`，不会给出误导性的 0。
- **参数面板**：代数区底部列出文档里的全部参数，可改值 / 最小 / 最大 / 步长 / 名称，并可新建与删除。动点自动生成的驱动参数会标注归属（显示为「驱动 A」），并在宿主对象被删除时自动回收，不留孤儿参数。
- **删除图形会连带删除它的交点**：交点、轨迹、连接、导函数/切线/积分区域等**纯派生对象**随来源一起删除，不再要求用户先手动清掉交点。反向地，**用户自己写下的内容仍然受保护**——注释、分组、约束、测量指向被删对象时会拒绝删除并说明原因。
- **两点之间的线段**：选中两个点（动点或普通点均可）后点「连接选中点」建立线段。它只保存两个点的引用、不存自己的坐标，因此永远不会过期；可被圆等曲线求交，交点随端点移动实时更新。
- 颜色、填充、线宽、透明度、虚线等公共样式编辑。
- 直线、圆、圆弧、圆锥曲线和函数的采样渲染。
- 参数滑块、表达式依赖 DAG、约束重算、撤销/重做和 `.mgeo` 保存/恢复。
- CAD「2D 绘图」具备可预期的手绘交互：坐标窗口**固定**为以原点为中心、只随视图比例缩放的方窗（不再按内容自适应，画下第一个点坐标系不会跳）；带主/次栅格（缩小时自动加粗，避免上百条线）；拖拽时显示**橡皮筋预览**与实时「长度 · 角度」读数；**对象捕捉**覆盖端点 / 交点 / 中点 / 圆心 / 象限点 / 垂足 / 切点 / 最近点（12px 捕捉半径，带捕捉标记与类型标签），优先级即此顺序——`最近点` 必须垫底，否则它在数学上永远最近会吃掉其他候选；命中多个候选时可用 **Tab 循环切换**；**栅格捕捉**为独立开关（对齐到最细可见网格，放大到能看见 1mm 次网格时精度随之提高）；角度约束按钮在「自由 → 正交 → 45° 极轴」间循环，正交始终压到轴上，**极轴仅在指针偏离射线 4° 以内才吸附**，按住 `Shift` 可临时正交。
- **工程图的线宽与命中带**：`vector-effect: non-scaling-stroke` 的宽度单位是**屏幕像素**，此前把工程图所有线宽写成 `0.022`／`0.04`，实测渲染成 `0.04px` 的隐形线、命中带同样只有约 `0.1px`（离中心 1px 就点不中）。现在线宽是 `1.4`／`1.6`（选中 `2.4`），并为绘图与投影图元各加一条 **14px 透明命中带**（内联样式给宽度，避免被更具体的选择器覆盖），细线也能点中。
- CAD「2D 绘图」的**命令条在图纸之外**：坐标输入、长度/角度、角度约束与栅格捕捉开关、偏移/修剪/延伸都渲染在图纸上方那一行工具条里（`DraftControlsRow`），不再挤进图纸内部的视口；绘图视口只负责画布与读数，读数（捕捉标签与「长度 · 角度」）按视图 `viewBox` 跨度换算字号，缩放图纸时不会被放大成巨大文字。工具条保持单行——一旦换行会占掉纸张高度（实测把纸张从 128% 压到 64%）。
- **工程制图可切换投影来源**：图纸默认投影自己的 CAD 文档，也可以切换为「立体几何」文档（两者是相互独立的工作区文档）。切换按钮显示当前来源（`投影来源：本图纸 / 立体几何`），空状态会说清是**哪份文档**为空、另一份里是否已有模型，并给出「去立体几何」的动作；**图纸布局（纸张、视图位置、比例）始终属于 CAD 文档**，切来源不会改图纸版式。
- CAD「2D 绘图」支持**夹点编辑**：选中图元后显示控制点（端点 / 折线顶点 / 圆心半径 / 圆弧起止角），拖控制点改几何、拖图元本体整体平移；拖动期间用临时文档做预览（端点移动时关联对象跟着重算），指针抬起才提交成一次可撤销的文档改动。夹点判定与数学画布共用 `interaction.ts` 的 `primitiveHandlePoints`/`getDragHandle`/`createDragAction`，仅在视口层把容差按屏幕像素换算（默认 0.35 世界单位在跨度 100 的绘图窗口里只相当于约 1.7px）。
- 直线—直线、直线—圆、圆—圆及采样曲线交点的分类、更新和异常回滚；采样曲线（函数与圆锥曲线）会返回**全部**交点，不再截断成前两个，病态输入以 `MAX_CURVE_INTERSECTIONS = 64` 为上限。**去重尺度只看图形自身有多大，与图形画在平面的哪里无关**：以前容差是"离原点多远"的 1e-4 倍，半径 1 的圆平移到 x≈1000 之后容差涨到 0.1，相距 0.05 的两个真实交点会被并成一个；现在容差是图形尺寸的 1e-9（并不小于 double 在该坐标量级下的分辨间隔），所以同一个图形画在原点、x=20 还是 x=100000，交点数都一样。
- **方向框选**：拖框方向决定语义——**左→右**为窗口选择（只选完全包含的对象，实线矩形），**右→左**为相交选择（选相交或包含的对象，绿色虚线矩形）。判定在内核 `selection.ts`：圆用"半径落在[最近距离, 最远距离]之间"判断圆周是否穿过框（大圆把框整个套住不算相交），无界直线/射线按定义端点判定，圆弧按渲染同样的 24 段采样。数学画布的平面基础图元同样支持两种语义，函数/圆锥曲线/派生曲线仍只支持"完全包含"（无解析框相交几何）。
- **Esc 分级取消**：第一次 `Esc` 取消进行中的创建/CAD 命令，第二次关掉操作指引，第三次才清空选择（不会误删或改动文档）。
- **创建优先的点选语义**：创建进行中点在图元上（含 14px 命中带）是**落点**，不是选中——与数学画布一致。此前绘图视口没跟这条语义，命中带一加宽就会出现"第二次点击落在已有图元附近 → 创建被静默取消、revision 不变、也没有任何报错"。
- **偏移 / 修剪 / 延伸**（2D 绘图视口工具栏）：**偏移**按给定距离**新建**一个平行对象（正值在行进方向左侧；折线按相邻偏移线求斜接点；圆/圆弧改半径且拒绝半径 ≤ 0；副本不继承 `slopeParameter`）；**修剪**保留目标 a 端所在的一半，**延伸**把目标 b 端拉到边界。修剪/延伸需要选中 2 个图元且**顺序是"先边界、后目标"**，目前只支持直线、线段、射线（圆/圆弧的修剪不在范围内，会明确报错）。几何在 `packages/geometry-kernel/src/editing.ts`，前置条件与补丁组装在 `apps/web/src/draftEditing.ts`，成败都在 CAD 状态栏给反馈。
- **命令行坐标与动态输入**：视口工具栏可直接键入落点——绝对 `12,34`、相对 `@10,5`、极坐标 `@20<45`（也接受空格分隔与全角逗号，中文输入法下不用切）；创建进行中额外提供「长度 / 角度」两个字段，只改其中一个、另一个沿用当前指针方向（AutoCAD 的动态输入语义），回车即按输入尺寸落点。输入非法时给出说明且**不会**落下错误的点。字段位置固定在视口工具栏而不是光标旁——图纸带 CSS zoom，光标旁浮层的定位与清晰度都不稳，这里换取可测可控。
- SVG、CSV、PNG 和 `.mgeo` 导出，以及浏览器级回归验证。
- `.mgeo` 支持浏览器下载保存、文件选择器打开和本地草稿恢复；当前不连接服务端文件上传接口。
- 可编辑函数公式框与计算器式公式键盘，支持 `e^x`、`sin(ln(x))`、绝对值、反三角、双曲函数和带底数的 `log_b(x)`；函数按钮按光标位置插入。圆锥曲线工作区提供「添加函数」入口和**常用函数预设**下拉（指数 / 对数 / 三角 / 双曲 / 高斯等，选择预设同时套用其课堂定义域），不再依赖手写完整表达式。
- 参数动态演变控制，支持播放、暂停、停止、循环、单次和往返模式；动画预览不逐帧污染撤销历史。
- 属性检查器按当前图元特异化显示，公共样式与几何关键属性分组呈现；中间画布会横向填充可用工作区，默认以 `(0, 0)` 为中心，支持鼠标中键拖动或 `Space + 左键拖动` 平移，并支持**滚轮以指针为中心缩放**与画布右下角的「放大 / 缩小 / 重置视图」按钮（缩放在 0.05×～40× 之间，网格步长随缩放自适应）。
- Ribbon UI 基线已完成：统一 Top Bar、工作区 Tab、可折叠/临时呼出/固定 Ribbon、动态状态栏和 100vh 响应式工作区；操作指引浮层避开底部状态栏。工作区显示名已改为「平面几何」，内部 ID 仍为 `conics`，`.mgeo` 文件格式不变。
- 右侧检查器不再展示几何约束与智能体（Agent）面板：`PropertiesBar` 只保留「几何参数 / 外观样式」两个手风琴分组，CAD 检查器只有数据 / 外观 / 工程标注三个页签；约束与测量的**文档数据、校验和 .mgeo 编码保持不变**，旧文件打开后约束记录仍在，教学测量入口移到默认可见的数据区。2026-09-16 审查中进一步删除了已无人引用的 `ConstraintPanel`（含测试）与 `.constraint-*` / `.agent-*` 孤立样式。
- 属性检查器把「图元名称」放在默认展开的「几何参数」区（原先只在需要先展开的「外观样式」里），重命名不再需要先找到外观页签。
- 平面工作区新建点使用 `A`、`B`、`C`…`Z` 课堂标签（`Z` 之后回退为 `P27`、`P28`…）；3D 空间点同样使用 `A`/`B`/`C`，并新增**画布内点标注层**把点名直接标在点位旁（标注层 `pointer-events: none`，不参与拾取，随相机旋转/缩放/窗口尺寸更新）。
- Ribbon 多模态组已收敛为「文字转换」和「图片转换」两个入口；项目还没有转换后端，两个入口因此保持**禁用**并在标题里说明「服务尚未接入，暂不可用」，不提供虚假的转换流程。
- CAD 工程工作台的左右停靠面板收起后，中间画布会吃掉空出的列宽（`.engineering-workbench` 暴露 `data-left-open` / `data-right-open`，其中 `workbench-body` 的网格列只保留可见面板）。
- 工程制图采用**制图台 + 图纸**的视觉层次：画布是带 24px 淡格线的冷灰台面，图纸是白纸，图框内缩并带分区刻度，右下角是真正的图签（工程图纸 / 图幅 / 比例 / 视图数 / 显示比例）。四个视图框用细线与双角定位刻度表达"视图"，不是白色卡片；视图标题压在图内左上角，视图比例与显隐按钮悬停时才出现（触摸设备常驻）。
- 图纸按制图台可用区域**等比缩放并居中**，并在工具栏提供「适应窗口 / 缩小 / 放大」与百分比读数：自动适应是默认，用户可以覆盖，放大超过一屏后图纸可滚动查看而不是被裁掉。缩放用 CSS `zoom`（参与布局），因此滚动范围与命中区域始终正确；`data-sheet-fit` / `data-sheet-scale` 暴露内部数值便于排查。图纸版式本身仍是文档状态，缩放只是显示适配。
- 3D 画布的「法向量」和「测量二面角」开关会把当前显示状态报给底部状态栏：法向量说明它画的是各可见面的外法向量；「测量二面角」会明确说明那是**坐标轴夹角的示例值**，并给出真实测量步骤（`Alt` 单选两个面，再点属性区的「二面角内角 / 二面角外角」）。
- 函数公式编辑框中的 Backspace/Delete 保留文本编辑行为，不会触发全局删除图元；画布悬停显示当前坐标。
- 点击功能键（添加图元、空间构造、测量、约束、函数分析等）后，画布**左下角**出现一条小尺寸操作指引：说明这一步该点什么、要按哪个键（`Esc` / `Shift` / `Alt`）或刚创建了什么。指引只在点击功能键时出现，切换工作区、`Esc`、点 `×` 或一步创建动作完成时消失，点另一个功能键会被替换；只选中对象不会弹出。浮层位于底部状态栏上方，不拦截状态栏操作。前置条件不足时给的是可执行做法（例如二面角提示「按住 Alt 点实体表面单独选面，Shift 加选第二个面」），而不是只说「选择不对」的报错；选中模板实体时提示 `Alt + 点击` 可单独选中棱或面。
- 函数采样遇到无定义值或疑似渐近线跳变时分段绘制，不会将无定义区间显示为竖直连线。
- 函数公式使用编译缓存；绘图、属性值域、导出和采样交点使用带细化上限的自适应采样。
- 函数内核支持一阶和二阶中心差分；无定义邻域返回明确的非有限结果，不继续计算伪导数。
- 函数分析内核支持数值零点、局部极值和拐点检测，并明确标记近似结果。
- 导函数图元保存来源函数 ID、阶数、定义域和采样参数；来源函数更新时由 Scene Graph 自动重算。
- 切线、法线和割线图元保存来源函数 ID 与计算参数，支持近似/无定义状态并随来源函数重算。
- 积分区域和分析结果集合可保存来源函数、计算区间、采样参数、结果点及诊断状态。
- 微积分**工作区标签**已于 2026-09-15 退役，但函数分析能力并未删除：当前圆锥曲线工作区可选中函数后直接「创建导函数 / 创建切线 / 创建积分区域」，属性面板继续显示导函数、切线、积分区域和分析集的来源与状态；相关 DSL 图元与 `.mgeo` 解码保留，含这些图元的旧文件仍可打开，且**函数与其派生分析对象作为一个对象一起删除**。后续 UI 计划将该界面标签改为「平面几何」，不改变内部 ID。
- P6 内核已具备不可变 `Vector3` 运算、三点定平面、射线拾取平面和二面角基础计算；Three.js 运行时依赖已加入 Web workspace。
- P6 DSL 已支持参数化立方体、棱锥、圆柱和圆锥，并保持 `schemaVersion: "0.1"` 与旧 2D `.mgeo` 解码兼容。
- P6-3 已接入原生 Three.js 3D 场景：参数化立方体按 DSL 尺寸和原点生成网格、边线、灯光、网格和坐标轴，并在 `geometry3d` 工作区提供创建入口；WebGL 不可用时显示明确降级状态。
- P6-4 已增加棱锥、圆柱和圆锥的参数化 Three.js 网格与工作区创建入口，四类实体均保留稳定 DSL ID、样式和选中高亮映射。
- 立体几何采用**世界 Z 轴朝上**的右手系（数学与工程制图惯例）：仰角绕 Z 抬起，网格平铺为 XY 地面，`AxesHelper` 的蓝色 Z 轴竖直向上；相机 `up` 显式设为 `(0,0,1)`。- P6-5 已支持 3D 画布左键旋转、中键平移、Shift+左键平移、Ctrl/Cmd+拖动沿视线纵深平移、滚轮缩放、视角重置和基于 Three.js Raycaster 的空间拾取。平移沿相机自身轴向生效（旋转后仍跟随指针），枢轴限制在图形包围盒周围的有限范围内；「平移视角」按钮可把左键拖动切换为平移，画布左下角常驻手势提示。
- P6-6 已支持透明面、隐藏边虚线、法向量箭头和实体选中高亮；三类显示状态均可从 3D 画布控件切换。
- P6-7 已支持保存来源实体 ID 与平面参数的截面对象；平面与立体棱求交生成截面点，来源实体变化后由 Scene Graph 自动重算，无交集时显示明确状态。
- P6-8 已支持立方体六面确定性展开布局、展开/折叠动画进度和画布控件。
- P6-9 已接入二面角测量显示，复用归一化法向量夹角内核，并明确展示示例法向量来源。
- P6-10 已将立体几何原点、尺寸、底面、高度、半径、分段数和**三轴朝向**接入属性栏与 Scene Graph patch 校验；Three.js 使用隔离挂载点保留 React 控件、重置按钮和降级状态。
- P6 v2-1 已增加点驱动三维 DSL：空间点、直线、线段、射线、平面、空间圆、棱、面和多面体支持稳定引用、退化校验与 mgeo round-trip；旧 2D 和四类参数化实体仍可读取。
- P6 v2-2 已增加不可变三维距离、混合积和共面判断；支持显式面环构造的棱柱、棱台、立方体、棱锥、圆柱/圆锥多边形近似，并通过 builder 注册表扩展自定义策略；开放边界、断开拓扑、自交面、非平面面和零体积输入返回明确诊断。
- P6 v2-3 已将 3D 点、线、面、棱和多面体引用接入 Scene Graph 反向依赖索引；支持不可变点位置 patch、沿空间直线/平面绑定点重算、拓扑来源删除保护和受影响对象局部传播。
- P6 v2-4 已接入点驱动课堂构造：空间点使用 A/B/C 等标签并支持 `x/y/z` 编辑；选中点可创建由稳定点 ID 驱动的空间直线、平面和面；Three.js 支持点、线、面渲染及元数据拾取。
- P6 v2-5 已将立方体、棱锥、圆柱和圆锥入口迁移为统一拓扑模板：保留旧参数源兼容性，同时生成稳定 ID 的空间点、棱、面和多面体；参数变化会同步拓扑点，编辑生成点后可脱离模板独立修改，旧文档加载时自动迁移。
- P6 v2-6 已接入空间拾取、空间约束与教学测量：Raycaster 命中按“点 > 棱 > 面 > 线 > 实体”优先级排序并按深度打破平局，结果携带稳定对象 ID、子部件 ID、深度和世界坐标。
- P6 v2-6 约束支持点在线、点在面、共线、共面、平行、垂直和固定距离（重合为二维约束）；属性栏只提供当前选择可求值的约束，并把“点在前”的来源顺序归一化，约束列表显示全部来源、残差和来源解释，冲突约束保留上一次合法状态，不静默移动无关点。
- P6 v2-6 测量支持长度、距离、角度、面积、体积和二面角，结果统一携带来源 ID、精度字段（当前计算结果一律标记为数值近似，不伪装成精确解）、状态（valid/degenerate/insufficient-data/numeric-failure）和计算依据；来源点移动后由 Scene Graph 自动重算，来源被测量引用时禁止删除，属性栏可删除测量以释放来源对象。
- P6 v2-6 代数区在立体几何工作区显示可展开的“顶点/棱/面”拓扑子树和“教学测量”分组，点击子树条目会同步选中 3D 场景对象；属性栏显示测量来源与精度。
- P6 v2-7 剖切支持任意点驱动多面体：截面来源既可以是 `polyhedron3` 拓扑对象，也可以是立方体、棱锥、圆柱和圆锥模板（优先使用模板已生成的拓扑）。平面按面边界求交，去重后按剖切平面内角度排序，并分类为无交、点交、线交和多边形交；退化输入不生成虚假点。
- P6 v2-7 新建截面的默认剖切平面穿过来源包围盒中心，因此四种模板和任意拓扑都能立刻看到有效截面；截面来源实体纳入删除保护，来源点移动后截面自动重算并保留点序。
- P6 v2-7 渲染按分类区分：多边形截面绘制半透明填充并叠加有序边界线，点交/线交退化截面绘制线段而不是面积。
- **截面 / 截线可以"看得见、点得到"**（2026-09-16，2026-09-17 起交线不再需要先选中两个对象，见上一条）：选中**单个**实体时画布给出默认剖切平面（穿过包围盒中心）的截面虚线预览（`默认剖切平面截面 · N 边形`）。悬停预览会高亮该对象并说明"点击创建为图元"，**点击即创建**持久化图元（进文档、可撤销、随来源重算）；顶点手柄优先于预览，棱不优先。
- **交点 / 交线 / 交面是三个各自独立的图元，自动铺开、点一下就建**（2026-09-17）：画布不再等用户选中两个对象，而是**自动枚举场景里所有两两相交的顶层实体**（`cube/pyramid/cylinder/cone/polyhedron3`；模板物化出来的点/棱/面/多面体不算独立实体，面与平面不参与，避免一个立方体的 6 个面互相刷出几十条噪声交线），每一对给出三类**可独立点击**的预览：
  - `pair:<a>|<b>:线` 虚线**交线**（两个表面的公共边界）→ 点它创建 `intersectionLine`（段数 / 总长度）；
  - `pair:<a>|<b>:面<i>` **交集的每一个面各一份**半透明**交面**片 → 点哪块建哪块，创建 `intersectionFace`（**一个表面**：面积、法向、顶点数，**填充色 / 描边色 / 透明度可改**）；
  - `pair:<a>|<b>:点<j>` **交线的每个拐点各一份**圆点标记 → 点它创建 `intersectionPoint3`（跟随两个来源重算的空间点）。
  完全包含时两个表面根本不相交：只有交面、没有交线与交点（这是数学事实，不是缺省）。状态栏会说明"已自动标出 N 处交线、M 处交点、K 个交面：点虚线创建交线，点圆点创建交点，点面片创建交面"。
- 三个图元都保存 `sourceIds: [string, string]` 并进依赖图；来源一移动就在同一事务里重算，进对象列表、可选中、可改名可改色、可单独隐藏，删掉任一来源会**级联注销**。身份靠 `hint` 稳定：交面按"离上一次形心最近的面"、交点按"离上一次位置最近的拐点"重新认领，所以来源移动后不会跳到对面去。
- `intersectionFace` 只画**一个**平面面片（半透明填充 + 它自己那圈边），顶点不单独画（交面的顶点就是交线的拐点，由交点那一路表达）。`intersectionPoint3` 画成一个单位球，半径由场景按屏幕尺寸统一缩放（与空间点手柄同一套约定），因此远看近看都一样大、都好点；它是派生对象，拖动不会改文档。
- `intersectionLine` 也补齐了检查器属性块：来源 A / 来源 B / 段数 / 总长度 / 状态（"有交线 / 无交线 / 数据不足"），有诊断时一并显示。
- `intersectionSolid`（**交集整体**表面）只作为**旧文档兼容**保留：读得进、算得出、画得出来（检查器给体积 / 表面积 / 面数 / 顶点数），但画布不再预览、也不再创建它——用户口径是"交面只是一个表面"。
- 性能：每一对按**解析后的几何签名**缓存（`intersectionPreviews3d.ts`），只动一个实体时其余对直接沿用上一次的结论；先用世界 AABB 筛掉不相交的对；单次扫描最多算 12 **对**来源的布尔交集（交线不受此限）、每对最多 64 个面、每对最多 32 个交点，候选上限 24 个实体 / 120 对。
- 自动枚举**只覆盖顶层实体**：按住 Alt 选中两个**面**（或一个面 + 一个实体）时，交线预览仍走选择驱动的老路径（`toSelectionLineScenePreview`），按同一套 key（`pair:<a>|<b>:线`）与自动预览去重——状态栏说"点击即可创建"时，画布上必定有那条可点的虚线。
- **按数值改过顶点的模板实体照常参与**：改一个模板顶点会把它的物化拓扑从 `construction.kind: "template"` 翻成 `"fromFaces"`，翻转时把"这条拓扑属于哪个实体"一并记进 `construction.sourceId`（DSL 有类型与校验）。因此数值编辑之后，该实体在截面、交线、交面里仍然找得到自己的拓扑；编辑真的把实体弄成非凸时，诊断说的是几何（"不是凸多面体…不做近似"），而不是"来源必须是实体"。
- 单次扫描的"没画全"分开报：配额用尽或面数超限 → "另有 N 对来源的交面没画全（一次最多算 12 对、每对最多 64 个面，改动来源后会补上）"，且**每次扫描都会重新报**（受限结果不当作完整结果缓存，配额腾出来就能补上）；实体对超出对数上限 → "另有 N 处相交对超出单次扫描上限…这次连交线都没画"。
- 指针落在预览上时"创建 vs 选中几何"的取舍由纯函数 `previewBeatsPick` 决定（有单测）：**面**片永远赢（指针确实落在画出来的那块面上）；**顶点手柄**默认优先，两个例外是截面预览（它的边界落在实体内部，不抢就永远点不到）与**交点标记**（交线的拐点常常就是来源的顶点，两者共心，用户点的是画出来的那个交点）；**棱**按深度判——预览在它前面（或同一深度）时预览赢（交线的命中带就画在那条棱上），预览明显在后面且指针确实压在棱上时才归那条棱，只是"擦过"的棱从不该赢。
- 同一处同时压着好几份预览时按"越具体越优先"取：**点 > 线 > 面**。
- 面环求交在内核 `intersections3d.ts`：两面环各自被对方平面裁剪成区间，再求两个区间的**交**（因此不会产生"只按点到直线距离筛点"造成的幽灵线）；共面直接短路（共面重叠没有唯一交线），重复线段去重；结果分类为 `intersection` / `section` / `none` / `insufficient`。布尔交集在内核 `boolean3d.ts`：凸实体 = 一组半空间的交集，用对方的每个面平面**依次裁剪**自己，切口面按平面内角度排序后补上。
- P6 v2-8 展开/折叠已泛化到任意点驱动拓扑：按共享棱邻接从根面递归展平，支持 0～1 的折叠进度、面朝向无关的展开方向判定和展开图重叠诊断；展开图每面可单独拾取并带 `unfolded-face-<序号>` 子部件 ID。
- P6 v2-8 折叠姿态仍是临时界面状态，不写入文档、不产生撤销记录，并在系统开启 reduced motion 时直接切换而不播放动画。
- P6 v2-9 二面角改为报告面内内角：以公共棱为轴取两个面内垂直于公共棱的方向计算夹角，结果与面的顶点顺序无关，并通过 `dihedralKind` 区分内角/外角；属性栏提供“二面角内角”和“二面角外角”两个入口，测量说明给出两个角度和公共棱。
- P6 v2-9 选中二面角来源面时，画布绘制公共棱、垂直于公共棱的角弧和两个朝外的法向量标记；内核同时提供点到直线、点到平面的垂足计算供后续投影标记复用。
- P6 v2-10 收起导出边界：立体几何工作区禁用 SVG 与 PNG 投影导出（此前会静默导出空图形），按钮带说明提示；平面工作区的 SVG/PNG 导出与所有工作区的 `.mgeo`、CSV 导出保持可用。
- P6 v3 测量可视化已完成：选中并创建有效三维测量后，3D 画布显示实际数值标签；二面角标签复用公共棱、角弧和法向量标记，属性栏明确区分内角与外角，失效测量不绘制虚假标记。
- P6 v2-10 点驱动 3D 文档的完整往返已验证：拓扑点/棱/面/多面体、带分类的截面、带内角/外角的二面角测量均可保存恢复，`schemaVersion` 保持 `"0.1"`。
- 新建点自动使用 A/B/C 等课堂标签；选择两个点后可创建保存点 ID 引用的动态线段连接。
- 点可绑定直线、线段、射线、折线、圆、圆弧或函数路径，并可记录参数域轨迹。
- 交点集合会显示所有数值交点位置（采样曲线对的上限为 64 个）；未被保存的交点会一直显示为可点击标记，保存其中一个只隐藏那一个，同一对图元的其他交点仍然可见；选中椭圆、双曲线或抛物线时画布标出中心、顶点和焦点。
- 抛物线连接必须提供第三点或完整顶点模型，两个点不会被自动猜成抛物线。
- **自由拖动**（2026-09-16）：画布显示控制新增「自由拖动」开关，开启后左键按住图形即可整体拖动，位移发生在**屏幕平面内**（深度不变，相机转过之后仍跟着指针走）。可拖对象为四类模板实体、空间点，以及由点驱动的线/线段/射线/棱/面/平面；**模板实体生成的点/棱/面不允许单独拖动**（它们由父级算出，单独拖会把实体悄悄拆散），绑定/派生点与锁定对象同样拒绝。拖动期间**不提交文档**，画面由临时偏移负责，抬手一次性提交——因此一次拖动就是一步撤销。方向键在自由拖动模式下微调选中截面的剖切面（1 个单位，Shift 走 0.2）。
- **拖动丝滑化**（2026-09-16）：修掉"一帧一帧"的真缺陷——位移只改了 Three.js 对象位置、**没有触发重画**，画面要等别的渲染顺带发生才更新（真实浏览器实测：20 次连续 pointermove 里只有 3 次真的重画）。现在每次移动都重画（实测 20/20），并在场景因选中/尺寸变化重建后把进行中的偏移补画回去，避免拖动中途回弹。
- **截面可发现性与剖切面可移动**（2026-09-16，2026-09-17 按用户反馈调整画法）：选中单个实体时画布给出**这一刀的交线（闭合虚线）与交点（圆点标记）**；剖切面本身只在"点出来的截面（交面）"与拖动刀口时出现——此前选中实体就铺一块半透明剖切面片盖在图形中间，用户反馈"我需要的是交面、交线和交点，而不是创建对象之后中间出现一个大截面"。指针指到那圈虚线时状态栏解释三者，**点击即创建**（命中区是那圈边界线，不需要瞄准 1px 虚线）。创建后选中截面，在自由拖动模式下**拖动截面**或按**方向键**即可沿法向平移剖切面，截面随剖切面实时重算；剖切面移出实体后不再保留过期形状，但剖切面本身仍可见，便于挪回来。
- **剖切面自由摆放**（2026-09-16）：属性面板新增「剖切面」区块（法向量 / 截面点数 / 分类读数 + 绕 X/Y/Z 各 ±15° 倾斜按钮），画布显示控制新增「取面」——点它再点实体上的某个面，该面即成为选中截面的剖切面（不共面的环会被拒绝）。旋转绕**实体中心**（而不是平面自身垂足）进行，否则刀口会被推出实体（实测平面到原点距离 1.5 → 0.15，截面消失）。**曾经的限制**"法向正好落在坐标轴上（如恰好 90°）时截面解析失败"**已修**（2026-09-17）：根因是旋转给出的法向带 ~1e-17 残差、同一个交点在不同面上算出的坐标差几个 ulp 而被当成不同点；点键改为按模型尺度量化后，默认水平面与「取面」选到的侧面平面都能正常成环。

### P6 v1 的边界

当前四类参数化实体是可用的教学工作区基线，不代表三维建模能力只限于立方体、棱锥、圆柱和圆锥。它们目前仍以模板入口为主，点、棱和面的通用创建与编辑将在 P6 v2 实施。

## 下一阶段功能

### P7 工程制图 MVP（已完成）

- P7-1 已完成投影内核：提供主视图、俯视图、左视图和轴测图的确定性正交基、二维坐标和深度值；非法三维坐标不生成投影结果。
- P7-2 已完成 renderer-neutral 投影描述：保留 DSL 稳定源 ID，输出点、棱折线和闭合面多边形，按深度和源 ID 确定性排序，并报告缺失引用/退化几何；参数化模板只渲染已物化拓扑。
- P7-3 已完成 CAD 四视图工作区：主视图、俯视图、左视图和轴测图使用响应式 SVG 面板，支持稳定源 ID 选择、空状态、诊断、键盘焦点和 CAD 安全工具栏。
- P7-4 已完成投影线联动与同步选择：可见空间点在当前视图与其他视图之间生成稳定来源的辅助投影线，四个面板共享源对象选中态，文档 revision 变化后自动刷新；投影线显示/隐藏为临时视图状态。
- 工程制图的投影来源可在「本图纸」与「立体几何」之间切换（2026-09-16 补齐）：此前投影只读 CAD 自己的文档，而两份工作区文档相互独立，所以从立体几何切到工程制图永远看到「暂无可投影的空间对象」且没有任何解释。现在来源是显式状态，空状态分别说明「本图纸没有可投影对象；立体几何里已有模型」或「立体几何工作区还没有可投影的对象」；二维图元与隐藏对象不计入可投影内容。
- P7-5 已完成工程标注与文档兼容：`.mgeo` 可选字段支持线性尺寸、角度、公差，保留来源 ID 并在内核计算有效值、退化/缺失状态；CAD 属性栏可从有效点/棱来源创建标注，四视图显示数值和诊断。
- P7-6 已完成工程图导出：SVG、DXF、PDF 统一消费 `ProjectedDrawing[]`，保留四视图、源 ID、标注和诊断；CAD 工具栏提供三种下载入口，3D 工作区继续禁用投影 SVG/PNG。
- 统一 Ribbon UI 后续优化（2026-09-16）均已完成：平面几何改名、A/B/C 点名与 3D 画布点名、图元重命名入口前移、右侧面板精简、CAD 画布随停靠面板收放、法向量/二面角底部提示；多模态入口保持禁用并说明服务未接入。
- 主视图、俯视图、左视图和轴测视图。
- 三维对象到工程视图的投影联动与视图切换。
- 工程图 SVG/DXF 导出，以及投影结果的可读性验收。
- 继续细化三维测量辅助标记的屏幕恒定缩放；选中态视觉层级已改为非破坏式（`face3` / `plane3` 保留用户填充色，选中提示由强调色外框与引导线承担）。

P7 的详细设计与切片验收见 [`2026-09-15-p7-engineering-drawing-design.md`](./superpowers/specs/2026-09-15-p7-engineering-drawing-design.md) 和 [`2026-09-15-p7-engineering-drawing.md`](./superpowers/plans/2026-09-15-p7-engineering-drawing.md)；P6 点驱动三维几何的历史设计与实施记录见 [`2026-09-14-point-driven-3d-geometry-design.md`](./superpowers/specs/2026-09-14-point-driven-3d-geometry-design.md) 和 [`2026-09-14-point-driven-3d-geometry.md`](./superpowers/plans/2026-09-14-point-driven-3d-geometry.md)。

### 函数

- 更丰富的公式键盘分类、符号模板和可选参数输入。
- 用户表达式与复合表达式，例如 `exp(-x^2) * cos(2*x)`、`log(x + 3)` 和参数化的 `a*sin(b*x+c)+d`。
- 定义域校验、采样密度和视口重采样。
- 函数的切线、割线、导数和积分标记作为派生对象。

### 点、标注和路径

- A/B/C/D 等可编辑标签、点的坐标显示、特征点自动标注和自定义注释。（平面与 3D 新建点已统一为 A/B/C 起始的字母标签，3D 点名已投影到画布，重命名入口在默认可见的数据区；由此派生出的“特征点自动标注”与自定义注释入口保持现状。）
- 点绑定自由坐标、线段/圆锥曲线/函数路径或其他图元的派生特征。
- 动点、轨迹采样、轨迹显隐和轨迹清除。
- 通过已标记点创建线段、直线、射线和折线；移动端点后连接关系持续有效。
- 抛物线连接必须补充顶点、轴向、焦参数或第三点等额外约束。

### 动态与分析

- 参数滑块、播放、暂停、停止、循环、单次和往返模式。
- 交点列表、交点坐标、解索引和切线交点状态。
- 圆锥曲线的中心、顶点、焦点、准线、离心率、渐近线和面积/周长等关键属性。
- 分析面板显示斜率、截距、定义域、值域、对称性和采样状态。

## 明确限制

- 等长/等角约束尚未纳入 DSL：为避免出现“能校验但算不出残差”的约束类型，本切片只声明点在线、点在面、共线、共面、平行、垂直和固定距离；等长/等角需要先确定角度表示，随空间关系教学切片加入。
- **三维约束当前在界面上不可创建、也不可查看**：2026-09-16 的右侧面板精简移除了「几何约束」手风琴与约束列表面板（原 `ConstraintPanel`，已删除），唯一的约束入口函数 `constraintOptionsFor` 因此没有任何界面调用点。**约束数据仍然完整保留**——`document.constraints`、`addConstraint`/`deleteConstraint` 等 Scene Graph 操作、`constraints3d` 残差与冲突诊断、`.mgeo` 编解码与校验都未改动，旧文件里的约束记录照常读写；缺的只是 UI。若要恢复，可按 `diagnoseConstraint3` + `constraintResidual` 重建面板（两者仍在内核中并有测试覆盖）。
- 三维约束目前只做诊断、残差与冲突解释，不做投影求解：`solvePoint3Constraints` 返回当前点位置与诊断，不会移动任何点；投影求解与内角/外角选择一并排入后续切片。
- 固定距离约束的残差内核已实现，但同样受上一条限制：界面既没有数值输入入口，也没有约束列表可查看。
- 二面角已按内角报告并可选外角，但尚不区分“有向二面角”的符号约定；同一对相邻面在不同顶点顺序下仍是同一个内角。
- 点到直线/平面的垂足计算已在内核实现并有测试，但画布还没有绘制垂足与投影辅助线，距离测量只显示数值。
- 剖切平面目前不可在界面中调整：新建截面使用穿过来源包围盒中心的水平面，用户还不能改法向量或偏移量。
- 布尔交集（交面）目前**只对凸实体成立**：L 形棱柱等非凸输入返回 `insufficient-data` 与"只对凸实体成立，这里不做近似"的诊断，而不是给一个看着像的错几何。圆柱与圆锥的面环是多边形近似，它们的布尔交集同样是近似的。
- 交面 / 交点预览的配额是硬编码的（单次最多 12 对来源、每对最多 64 个面 / 32 个交点）：实体多且两两相交时后面的只画一部分，状态栏会说明，但还没有"按音量排序 / 只画视锥内"的取舍。
- `intersectionSolid`（交集整体）只剩旧文档兼容：新文档不会再创建它，展开图与二面角测量也**不**把 `intersectionFace` / `intersectionPoint3` / `intersectionSolid` 算进去。
- 截线/截面**不做布尔运算**，也不把实体真实切开显示内部：只提取并绘制交线，实体本身完整保留。
- 两个实体相交时给的是**面环之间的真实交线**，不为"任意两实体"猜一个剖切平面——想用默认剖切平面就先只选中一个实体。两个面**共面**时没有唯一交线，此时不预览。
- 圆柱与圆锥的面环是**多边形近似**，因此它们参与的交线/截面也是近似的；精确的二次曲面求交不在当前范围内。
- 展开状态目前不持久化：折叠进度是临时界面状态，`.mgeo` 不记录“已展开”，重新打开文件后回到折合姿态。
- 展开图重叠只做诊断不做自动布局：出现重叠时在诊断里说明是哪两个面，不会重新排布根面或面序。
- 未生成拓扑的旧截面文档走模板棱表回退路径，其交点按同一套平面内角度规则排序，因此新旧路径的截面点序一致。
- 两个点本身不能唯一确定一般抛物线；产品不会悄悄猜测一条抛物线。
- 采样函数和曲线的交点是数值近似，必须显示“采样/近似”状态，不能伪装成符号精确解。
- 采样曲线对的交点列表有 64 个上限：`sin(1/x)` 这类在视口内近乎无限次相交的病态函数不会无限增长标记，超过上限时只保留前 64 个数值解。
- 删除一个函数会连带删除它的导函数、切线、法线、割线、积分区域和分析集（它们是同一个对象）；但被**保存交点**引用的图元仍受来源删除保护，需要先删除引用它的交点。
- 极点、渐近线和无穷远交点不作为普通有限坐标点写入 `.mgeo`。
- 动画帧不逐帧产生撤销记录；只有停止、确认或手动改值才提交文档。撤销/重做可用按钮与 `Ctrl/Cmd+Z`、`Ctrl/Cmd+Shift+Z`、`Ctrl/Cmd+Y` 触发（文本框内不抢键），无可撤销内容时按钮为禁用态；刷新后草稿恢复的是文档内容，历史不保留，因此此时撤销按钮为禁用。
- 任意 JavaScript 表达式、网络加载脚本和不受限用户代码不属于函数输入能力。
- 平面（`plane3`）画出来的是**有界**面片：默认按所属图形的包围盒自适应，也可在属性面板填入「半边长」固定尺寸（「恢复自动」清除该值）。仍没有在画布上拖动平面边界的手柄，也不能隐藏面片只保留平面定义。
- 四类参数化模板（立方体/棱锥/圆柱/圆锥）的朝向由可选 `rotation`（弧度，X → Y → Z）描述，属性面板按度输入并提供 0/90° 快捷角度；模板生成的顶点**只在模板自己的参数变化时才按模板重建**（2026-09-17 起），因此手工调过的顶点不会被无关改动拽回去，而"全量重算"仍会按参数把它修复。定向请用朝向参数。
- 在三维画布上点击**模板实体**（立方体/棱锥/圆柱/圆锥）的棱或面，默认选中整个实体（这是让实体可被点选的关键规则）；需要选具体棱/面时按住 **Alt 点击**即可，也可继续在左侧代数区展开拓扑后选取。模板生成的**顶点**仍可直接点选，保留点驱动编辑。
- `circle3` 目前只在 DSL 与内核中存在，画布没有渲染分支；当前没有界面入口可以创建它，因此不影响课堂流程。
- 三维视角的移动范围被有意限制：枢轴只能在以当前图形包围盒中心、半径为其 3 倍的范围（无几何时为原点周围 ±12）内平移，避免把图形拖出画面；需要更大范围可先「适应视图」或移动图形本身。

### 体检结论与已知限制（2026-09-17）

- **"实体内"（`inSolid`）宿主只对凸实体提供**：凹实体（例如 L 形棱柱）不再返回宿主——夹取几何在凹体上会停在空腔里、甚至把绑定点留在空中，因此宁可让约束报"数据不足"。面平面朝向改用**有符号体积**判定（不再用可能落在体外的顶点形心），面法向的退化判据也随实体尺度缩放，因此毫米 / 微米量级的实体照常可用。绕向自相矛盾的拓扑同样被拒绝。
- **退化直线上的平行 / 垂直 / 共线约束被跳过**：求解结果把它们列在 `unsatisfiable` 里（不再假装"已满足"），但**不会**让求解失败——`recomputeDerivedObjects` 在 `!converged` 时抛错，若把退化也算失败，"把一条被约束的线拖成一个点"就会让整份文档报错。
- **剖切平面的法向必须有效**：零法向（或相对模型尺度可忽略）以前会"凭空造出一片截面"并报 `polygon`，现在返回 `insufficient-data`，不产出任何点。
- **顺时针圆弧的参数域是升序区间** `[endAngle, startAngle]`，投影参数也折回该区间；滑块与动点因此不会一拖就跳到端点。
- **PDF 导出只支持 WinAnsi 字符**：中文注释与中文诊断会被替换成 `?`（导出成功、内容如实标出"有字打不出来"）。真正的 CJK 需要内嵌字体（体积与许可证都要考虑），尚未实现。
- **不可读的草稿会被保留**：能解析成 JSON 但当前版本解不出来的草稿挪到 `mathcanvas:draft:<工作区>:unreadable` 旁路键并提示用户；只有"根本不是 JSON"的垃圾数据才会被清掉。多标签页 / 打开文件覆盖草稿仍**没有**版本比对（需要 `storage` 监听与修订号语义）。
- **e2e 现在构建当前工作区**：`e2e/global-setup.mjs` 按 `apps/web` 的 vite 配置重新构建 `build-check/mathcanvas-current` 再起 preview。之前它直接 preview 这个可写目录，实测跑的是几天前的旧 bundle——"Playwright 全绿"曾与源码无关。用户在 127.0.0.1:4175 上手工验收的预览也是同一个目录（刷新页面即取到新产物）。
- **仍未修的性能项**：`boolean3d` 的顶点索引按字符串键线性重建（O(V²)）、`DrawingViewport` 移动阈值会丢弃编辑、每帧自动保存草稿（已确认不会丢数据，属体验优化）。
- **角度标注（两条棱）在公共端点处测量**：折线拐角（一条棱的终点正好是另一条棱的起点）量的是那个角；两条棱没有公共端点、或完全重合时没有唯一可量的角，报"数据不足"而不是给一个错的度数。
- **工程图导出不再限制视图数量**：SVG 画布高度按行数增长、PDF 每个视图一页、DXF 直接列出全部视图（此前三者都硬写"最多 4 个"，第 5 张起会被静默丢掉）。
- **导出 CSV 带 UTF-8 BOM**：Excel（Windows）在没有 BOM 时会按本地 ANSI 解码，中文标签会变成乱码。
- **`persistence/mgeoStorage.ts` 已删除**：它只是 `encodeMgeo` / `decodeMgeo` 的转发壳，全仓库没有生产调用点；原先经它执行的 3 个往返用例改为直接调用真实编解码入口（`mgeoRoundTrip.test.ts`），覆盖不变。
- **仓库无乱码注释**：体检时按字节扫描了 280 个源文件与文档的乱码特征字符，没有任何命中（此前在终端里看到的中文乱码是 `Get-Content` 按 ANSI 解码的**输出**问题，不是磁盘内容；仓库改文件仍一律用编辑工具）。
- **射线交点判据随坐标尺度**：`onRay` 的垂距容差改为 `scaledTolerance`，在 ~1e6 的坐标上不再把"确实相交"误判成"交点在射线起点之后"（真正落在起点之后的解仍然被挡掉）。
- **展开（`unfoldPolyhedron3`）的面法向用整个环的 Newell 法向**：环上前三点共线是合法多边形，旧实现取前三点叉积会得到零向量、把展开角当成 0 度、让侧面留在折合姿态却报 `ok`；现在只有环真的零面积才失败，并且是**明确报数据不足 + 诊断**（"环无法确定法向（面积为 0），展开角不可解"），不再静默折合。
- **死代码清理：`store.ts` 的预览三件套已删除**（`beginPreview` / `previewParameter` / `commitPreview` / `cancelPreview` 与 `previewBase` 全仓库没有调用点，且 `apply` 会清掉 `previewBase`，预览中途再改一次永远回不到基线）。
- **布尔交集的顶点索引改为哈希表**：`clipByPlane` 原来每插入一个顶点就线性扫描并重建字符串键（O(F·V) 次键构造）。实测两个圆柱近似的**一次**交集：48 段 137ms → 27ms、96 段 1025ms → 83ms（体积逐位一致）。大体量用例（6 次 96 段交集）现在是**性能回归的软守卫**——旧实现在默认 5s 测试超时内跑不完。
- **相机记忆按文档保存**（最多 4 个，最近使用的优先保留）：此前只有一个槽位，交替打开两个 3D 文档会让前一个的视角被覆盖掉；记忆仍只在内存里，刷新回到默认视角。
- **工程标注不抢指针**：投影视图里的尺寸 / 公差文本加了 `pointer-events: none`，标注压在图元上时点击仍然落到图元（标注本身没有点击处理，只是读数）。
- **草稿夹点可用键盘操作**：已选图元的夹点是可聚焦的按钮（`role="button"` + `aria-label` 读对象名），方向键按 1 个世界单位移动、Shift 加速 10 倍，走与鼠标拖动同一条提交路径（因此撤销粒度一致）。
- **`Tab` 不再是键盘陷阱**：多个捕捉候选重叠时的 `Tab` 循环只接管**不带 Shift** 的按键，Shift+Tab 交还浏览器，键盘用户可以走出绘图视口（WCAG 2.1.2）。
- **内核容差与输入护栏（2026-09-17 体检第六批）**：`polyline.pointOnRay` 的垂距判据随坐标尺度缩放（~1e9 坐标上不再误判）；只有一个点的折线，`distanceToPolyline` 返回"到那个点"的距离而不是 `+Infinity`；`buildSolid` 的内部异常诊断会带上原始消息（不再把内部崩溃说成"输入不合法"）；`MAX_SOLID_SEGMENTS = 256` 卡住分段数上界（`segments: 1e9` 不再去分配十亿个顶点，与 DSL schema 的上限一致）。
- **自动保存的实测结论**：`encodeMgeo` 单次开销在示例文档上 0.029ms、200 图元 0.219ms、3000 图元 2.631ms——课堂规模的文档下可忽略，因此**不**为它加防抖（防抖会削弱"刷新即恢复草稿"的保证）。"一次按键一条撤销记录"是既有测试钉住的设计（历史上限 100 条），不是缺陷。
- **折线偏移要求每段都有方向（2026-09-17 体检第七批）**：带重复顶点的折线（连续两点重合）偏移时返回 `null`，不再抛 `TypeError`（旧实现只给末段判空）。
- **平面法向必须真的能归一化**：`(1e-30,0,0)` 这类数量级的法向在加载时仍算"非零"，但归一化会得到零向量——此时点到平面的距离测量报 `insufficient-data`、点面约束的残差为 `null`（不再把它当成"距离 0 / 永远满足"）。非单位但可归一化的法向（如 `(0,0,5)`）照常工作。

## 参考来源

同类项目的调研、许可证边界和借鉴决策见 [`docs/research/graphing-tools.md`](./research/graphing-tools.md)。
